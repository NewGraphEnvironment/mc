% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_deliver.R
\name{mc_deliver}
\alias{mc_deliver}
\title{Build and deliver a message (internal)}
\usage{
mc_deliver(
  path = NULL,
  to,
  subject,
  cc = NULL,
  bcc = NULL,
  from = default_from(),
  thread_id = NULL,
  .draft,
  to_self = FALSE,
  sig = TRUE,
  sig_path = NULL,
  attachments = NULL,
  labels = NULL,
  labels_create = TRUE,
  html = NULL,
  send_at = NULL,
  scheduler = c("callr", "auto", "launchd", "at")
)
}
\arguments{
\item{path}{Path to the markdown draft file. Passed to \code{\link[=mc_md_render]{mc_md_render()}}.}

\item{to}{Recipient email address (character string or vector).}

\item{subject}{Email subject line.}

\item{cc}{Optional CC recipients (character vector). Default \code{NULL}.}

\item{bcc}{Optional BCC recipients (character vector). Default \code{NULL}.}

\item{from}{Sender address. Default uses \code{getOption("mc.from")},
then the \code{MC_FROM} environment variable. Errors if neither is set.}

\item{thread_id}{Gmail thread ID to reply into. Default \code{NULL} (new thread).
Use \code{\link[=mc_thread_find]{mc_thread_find()}} to look up thread IDs.}

\item{.draft}{Logical. \code{TRUE} creates a Gmail draft, \code{FALSE} sends.
Supplied by the wrapper, never by the caller.}

\item{to_self}{Logical. If \code{TRUE}, override \code{to} with \code{from} and drop \code{cc},
\code{bcc} and \code{thread_id}, so the message reaches nobody but the sender.
Default \code{FALSE}. This caps who receives the message; it does not stop the
send. Use \code{\link[=mc_draft]{mc_draft()}} if you do not want a message delivered at all.}

\item{sig}{Logical. Append signature? Passed to \code{\link[=mc_md_render]{mc_md_render()}}.
Default \code{TRUE}.}

\item{sig_path}{Path to a custom signature HTML file. Default \code{NULL}
uses the bundled New Graph signature. Passed to \code{\link[=mc_md_render]{mc_md_render()}}.
Ignored when \code{sig = FALSE} or when \code{html} is provided.}

\item{attachments}{Optional character vector of file paths to attach.
Each file is attached via \code{\link[gmailr:gm_mime]{gmailr::gm_attach_file()}}. Default \code{NULL}.}

\item{labels}{Optional character vector of Gmail label names to apply
to the resulting thread. Applied via \code{\link[=mc_thread_modify]{mc_thread_modify()}} after a
successful send.}

\item{labels_create}{Logical. When \code{TRUE} (default), missing user
labels in \code{labels} are auto-created via \code{\link[=mc_label_ensure]{mc_label_ensure()}}.}

\item{html}{Optional pre-rendered HTML body. If provided, \code{path} is ignored
and this HTML is used directly.}

\item{send_at}{Schedule the email for later. Either a \code{POSIXct} datetime
or a numeric number of minutes from now. Default \code{NULL} (send now).
Scheduling is inherently a send, which is why it lives here and not on
\code{\link[=mc_draft]{mc_draft()}}.}

\item{scheduler}{Backend for scheduled send. One of \code{"callr"} (default),
\code{"auto"} (OS-native: \code{launchd} on macOS, \code{at} on Linux), \code{"launchd"}, or
\code{"at"}. Ignored when \code{send_at} is \code{NULL}.}
}
\value{
Gmail thread ID, invisibly.
}
\description{
Shared implementation behind \code{\link[=mc_draft]{mc_draft()}} and \code{\link[=mc_send]{mc_send()}}. Renders the body,
builds the MIME message, and either creates a Gmail draft or sends,
according to \code{.draft}.
}
\details{
Not exported. The exported wrappers each fix \code{.draft}, which is what makes
"nothing passed to \code{mc_draft()} can send" true by construction rather than
by convention (#39).
}
\keyword{internal}
