% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_md_render.R
\name{mc_md_render}
\alias{mc_md_render}
\title{Render a markdown draft to HTML email body}
\usage{
mc_md_render(path, sig = TRUE)
}
\arguments{
\item{path}{Path to the markdown draft file.}

\item{sig}{Logical. Append the standard signature? Default \code{TRUE}.}
}
\value{
A character string of HTML ready for \code{\link[=mc_send]{mc_send()}}.
}
\description{
Reads a markdown file, strips everything above the first \verb{---} separator
(header metadata), converts the remaining markdown to HTML, inlines table
styles for Gmail compatibility, and appends the New Graph signature.
}
\details{
Gmail strips \verb{<style>} blocks from HTML emails. This function adds inline
styles to \verb{<table>}, \verb{<th>}, and \verb{<td>} elements so tables render correctly.

The markdown file should follow the compost template format:

\if{html}{\out{<div class="sourceCode">}}\preformatted{# Email to Recipient - Topic

**Subject:** ...

**To:** ...

---

Hi Name,

Body text here.
}\if{html}{\out{</div>}}

Everything above and including the \verb{---} line is stripped. Everything below
is converted to HTML.
}
\examples{
\dontrun{
html <- mc_md_render("communications/20260222_brandon_cottonwood_draft.md")
cat(html)
}

}
