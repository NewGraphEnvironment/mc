% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_message_find.R
\name{mc_message_find}
\alias{mc_message_find}
\title{Search Gmail at the message level}
\usage{
mc_message_find(
  query,
  n = 10,
  after = NULL,
  before = NULL,
  status = c("any", "sent", "draft")
)
}
\arguments{
\item{query}{Gmail search query.}

\item{n}{Maximum number of results. Default \code{10}.}

\item{after, before}{Optional date filters. \code{Date} or \code{"YYYY-MM-DD"}
string. See \code{\link[=mc_thread_find]{mc_thread_find()}}.}

\item{status}{One of \code{"any"} (default), \code{"sent"}, or \code{"draft"}. \code{"sent"}
restricts to non-draft messages; \code{"draft"} searches drafts instead of
messages.}
}
\value{
A data frame with columns \code{message_id}, \code{thread_id}, \code{from}, \code{to},
\code{subject}, \code{date}, and \code{status}, most recent first. Returns an empty
data frame (same columns) when no messages match.
}
\description{
Like \code{\link[=mc_thread_find]{mc_thread_find()}} but returns one row per message rather than per
thread. Useful when timed sends, re-threads, or manually moved drafts
scatter a logical conversation across multiple thread IDs and you want
to locate a specific message directly.
}
\examples{
\dontrun{
mc_message_find("to:cindy newsletter", after = Sys.Date() - 1)
mc_message_find("subject:invoice", status = "sent")
mc_message_find("subject:draft-only", status = "draft")
}

}
