% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_md_draft.R
\name{mc_md_draft}
\alias{mc_md_draft}
\title{Create a Gmail draft from a markdown file with YAML frontmatter}
\usage{
mc_md_draft(path, to_self = FALSE, override = list())
}
\arguments{
\item{path}{Path to the markdown draft (with YAML frontmatter).}

\item{to_self}{Logical. If \code{TRUE}, address the message to the sender and
drop \code{cc}, \code{bcc} and \code{thread_id}, so it reaches nobody else. Default
\code{FALSE}. Caps the recipients; does not stop the send.}

\item{override}{Named list of arguments to override frontmatter values
at call time (e.g. \code{list(subject = "New subject")}). Overrides merge
\strong{after} frontmatter, so \code{override} wins.}
}
\value{
Invisibly returns whatever \code{\link[=mc_draft]{mc_draft()}} returns.
}
\description{
Reads metadata (\code{to}, \code{subject}, optional \code{cc}, \code{bcc}, \code{thread_id},
\code{attachments}, \code{labels}, \code{from}) from the YAML frontmatter at the top of a
markdown draft and dispatches to \code{\link[=mc_draft]{mc_draft()}}.
}
\details{
Drafts only. Nothing passed here sends — use \code{\link[=mc_md_send]{mc_md_send()}} to deliver.

Required frontmatter fields: \code{to}, \code{subject}. Missing either triggers an
error that names the file.

A \code{thread_id} in the frontmatter warns: \code{gmailr::gm_create_draft()} cannot
thread a draft. See \code{\link[=mc_draft]{mc_draft()}}.
}
\examples{
\dontrun{
mc_md_draft("communications/20260413_cindy_newsletter_draft.md")
}

}
\seealso{
\code{\link[=mc_md_send]{mc_md_send()}} to send, \code{\link[=mc_draft]{mc_draft()}} for the argument-driven form.
}
