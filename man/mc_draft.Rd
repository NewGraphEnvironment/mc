% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_draft.R
\name{mc_draft}
\alias{mc_draft}
\title{Create a Gmail draft from a markdown file}
\usage{
mc_draft(
  path = NULL,
  to,
  subject,
  cc = NULL,
  bcc = NULL,
  from = default_from(),
  thread_id = NULL,
  to_self = FALSE,
  sig = TRUE,
  sig_path = NULL,
  attachments = NULL,
  labels = NULL,
  labels_create = TRUE,
  html = NULL,
  ...
)
}
\arguments{
\item{path}{Path to the markdown draft file. Passed to \code{\link[=mc_md_render]{mc_md_render()}}.}

\item{to}{Recipient email address (character string or vector).}

\item{subject}{Email subject line.}

\item{cc}{Optional CC recipients (character vector). Default \code{NULL}.}

\item{bcc}{Optional BCC recipients (character vector). Default \code{NULL}.}

\item{from}{Sender address. Default uses \code{getOption("mc.from")},
then the \code{MC_FROM} environment variable. Errors if neither is set.}

\item{thread_id}{Gmail thread ID to reply into. Default \code{NULL} (new thread).
Use \code{\link[=mc_thread_find]{mc_thread_find()}} to look up thread IDs.}

\item{to_self}{Logical. If \code{TRUE}, address the draft to \code{from} and drop
\code{cc}, \code{bcc} and \code{thread_id}. Default \code{FALSE}.}

\item{sig}{Logical. Append signature? Passed to \code{\link[=mc_md_render]{mc_md_render()}}.
Default \code{TRUE}.}

\item{sig_path}{Path to a custom signature HTML file. Default \code{NULL}
uses the bundled New Graph signature. Passed to \code{\link[=mc_md_render]{mc_md_render()}}.
Ignored when \code{sig = FALSE} or when \code{html} is provided.}

\item{attachments}{Optional character vector of file paths to attach.
Each file is attached via \code{\link[gmailr:gm_mime]{gmailr::gm_attach_file()}}. Default \code{NULL}.}

\item{labels}{Optional character vector of Gmail label names to apply
to the resulting thread. Applied via \code{\link[=mc_thread_modify]{mc_thread_modify()}} after a
successful send.}

\item{labels_create}{Logical. When \code{TRUE} (default), missing user
labels in \code{labels} are auto-created via \code{\link[=mc_label_ensure]{mc_label_ensure()}}.}

\item{html}{Optional pre-rendered HTML body. If provided, \code{path} is ignored
and this HTML is used directly.}
}
\value{
The Gmail thread ID of the resulting draft, invisibly. May be \code{NULL}
if the gmailr response did not include one.
}
\description{
Creates a draft for review. Nothing passed to this function sends — to
deliver a message, call \code{\link[=mc_send]{mc_send()}}.
}
\details{
\code{gmailr::gm_create_draft()} does not accept a \code{thread_id}, so drafts always
land outside the conversation. Supplying \code{thread_id} warns. Either move the
draft into the thread from the Gmail UI before sending, or use \code{\link[=mc_send]{mc_send()}}
to deliver straight into the thread.

\code{send_at} and \code{scheduler} are not accepted here. A scheduled message is by
definition one that gets sent, so scheduling lives on \code{\link[=mc_send]{mc_send()}}.
}
\examples{
\dontrun{
mc_draft("communications/newsletter.md",
         to = "someone@example.com",
         subject = "Spring newsletter")

# Draft addressed to yourself, to check the rendering
mc_draft("communications/newsletter.md",
         to = "someone@example.com",
         subject = "Spring newsletter",
         to_self = TRUE)
}

}
\seealso{
\code{\link[=mc_send]{mc_send()}} to send, \code{\link[=mc_md_draft]{mc_md_draft()}} to draft from frontmatter.
}
