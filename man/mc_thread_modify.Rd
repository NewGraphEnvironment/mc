% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_thread_modify.R
\name{mc_thread_modify}
\alias{mc_thread_modify}
\title{Modify a Gmail thread's label state (archive, trash, star, label, etc.)}
\usage{
mc_thread_modify(thread_id, add = NULL, remove = NULL)
}
\arguments{
\item{thread_id}{Gmail thread ID (e.g. from \code{\link[=mc_thread_find]{mc_thread_find()}}).}

\item{add}{Character vector of label names to add. \code{NULL} for none.}

\item{remove}{Character vector of label names to remove. \code{NULL} for none.}
}
\value{
Invisibly returns the gmailr response.
}
\description{
Adds and/or removes labels on a thread in one call. Accepts user-defined
label names or Gmail system labels (INBOX, STARRED, UNREAD, IMPORTANT,
TRASH, SPAM, SENT, DRAFT). Covers common operations beyond "labeling":
archive (\code{remove = "INBOX"}), star (\code{add = "STARRED"}), trash
(\code{add = "TRASH"}), mark-read (\code{remove = "UNREAD"}).
}
\details{
Pass \strong{names}, not IDs. User-label names resolve to opaque IDs via
\code{\link[gmailr:gm_labels]{gmailr::gm_labels()}}. System labels pass through unchanged. When a user
label's name collides with a system label ID (e.g. a user label named
\code{"STARRED"}), the system interpretation wins — almost always what's
intended.

At least one of \code{add} or \code{remove} must be non-\code{NULL}. The \code{gm_labels()}
call is skipped entirely when every input is a system label.
}
\examples{
\dontrun{
# Apply a user label
mc_thread_modify("18171fb2cec08e9d", add = "Clients/Acme")

# Archive and mark read in one call
mc_thread_modify("18171fb2cec08e9d", remove = c("INBOX", "UNREAD"))

# Star a thread
mc_thread_modify("18171fb2cec08e9d", add = "STARRED")

# Status transition
mc_thread_modify("18171fb2cec08e9d", add = "Done", remove = "Pending")
}

}
