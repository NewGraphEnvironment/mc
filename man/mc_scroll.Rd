% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_scroll.R
\name{mc_scroll}
\alias{mc_scroll}
\title{Wrap a table in a scrollable container for email}
\usage{
mc_scroll(table, direction = "both", max_height = "400px")
}
\arguments{
\item{table}{A kable/kableExtra object or a character string of HTML.}

\item{direction}{Scroll direction: \code{"wide"} (horizontal), \code{"long"}
(vertical), or \code{"both"}. Default \code{"both"}.}

\item{max_height}{Maximum height before vertical scrolling kicks in.
Default \code{"400px"}. Ignored when \code{direction = "wide"}.}
}
\value{
A character string of HTML with the table inside a scrollable div.
}
\description{
Wraps HTML table output in a \verb{<div>} with \code{overflow} CSS so large
tables scroll horizontally, vertically, or both in Gmail.
}
\examples{
\dontrun{
df <- data.frame(x = 1:50, y = rnorm(50))

# Vertical scroll
mc_scroll(knitr::kable(df, format = "html"), direction = "long")

# Horizontal scroll
mc_scroll(knitr::kable(wide_df, format = "html"), direction = "wide")

# Use with mc_compose
body <- mc_compose(
  "<p>Here's the data:</p>",
  mc_scroll(knitr::kable(df, format = "html"))
)
}

}
