% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_thread_find.R
\name{mc_thread_body_latest}
\alias{mc_thread_body_latest}
\title{Return the latest top-level message body in a thread}
\usage{
mc_thread_body_latest(
  thread_id,
  strip_quotes = TRUE,
  status = c("any", "sent", "draft")
)
}
\arguments{
\item{thread_id}{Gmail thread ID.}

\item{strip_quotes}{Logical. If \code{TRUE} (default), remove lines starting
with \code{>} plus the \code{"On ... wrote:"} attribution line that Gmail inserts
above quoted history.}

\item{status}{One of \code{"any"} (default), \code{"sent"}, or \code{"draft"}. Restricts
the pool of messages considered when selecting the latest.}
}
\value{
A single character string with the latest body (or \code{""} if none).
}
\description{
Convenience wrapper over \code{\link[=mc_thread_read]{mc_thread_read()}} that pulls the most recent
message in a thread and, by default, strips quoted reply history so you
get just what was actually written at the top. Useful for recording what
was sent, comparing draft vs sent, or scanning thread activity.
}
\examples{
\dontrun{
mc_thread_body_latest("19cd3565c3161b4b")
mc_thread_body_latest("19cd3565c3161b4b", strip_quotes = FALSE)
}

}
