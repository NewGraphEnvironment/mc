% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_label_ensure.R
\name{mc_label_ensure}
\alias{mc_label_ensure}
\title{Ensure Gmail user labels exist (create any missing)}
\usage{
mc_label_ensure(label_names)
}
\arguments{
\item{label_names}{Character vector of Gmail label names to ensure exist.}
}
\value{
Invisibly returns \code{label_names}.
}
\description{
Looks up existing user labels via \code{\link[gmailr:gm_labels]{gmailr::gm_labels()}} and creates any
name in \code{label_names} that doesn't yet exist. No-op for labels already
present. System label names (INBOX, STARRED, etc.) are skipped — those
are built into Gmail and cannot be created. System-label match is
case-insensitive so \code{"Inbox"}, \code{"inbox"}, and \code{"INBOX"} are all skipped.
}
\details{
Useful for the YAML-driven label workflow in \code{\link[=mc_send]{mc_send()}} / \code{\link[=mc_md_send]{mc_md_send()}}:
new project tags can land in frontmatter without needing to be created
in the Gmail UI first. Also handy for seeding a project's full label set
up front.
}
\examples{
\dontrun{
# Tag-as-you-go: works even if "project-foo" doesn't exist yet
mc_label_ensure(c("project-foo", "urgent"))
mc_thread_modify("18171fb2cec08e9d", add = c("project-foo", "urgent"))

# Seed a project's labels in one shot
mc_label_ensure(c("client/Acme", "client/Acme/in-flight",
                  "client/Acme/done"))
}

}
