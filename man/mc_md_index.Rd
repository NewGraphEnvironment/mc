% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_md_index.R
\name{mc_md_index}
\alias{mc_md_index}
\title{Index markdown email drafts under a directory}
\usage{
mc_md_index(
  dir = "communications",
  pattern = "_draft\\\\.md$",
  recursive = TRUE
)
}
\arguments{
\item{dir}{Directory to scan. Defaults to \code{"communications"}.}

\item{pattern}{Regex matched against filename (not path). Default
\code{"_draft\\\\.md$"} matches compost draft naming.}

\item{recursive}{Logical. Recurse into subdirectories? Default \code{TRUE}.}
}
\value{
A data frame with columns \code{path}, \code{date}, \code{to}, \code{cc}, \code{subject},
\code{thread_id}, \code{has_attachments}. \code{date} is parsed from an 8-digit
\code{YYYYMMDD_} prefix in the basename when present, else \code{NA}. Multi-value
fields (\code{to}, \code{cc}) are collapsed with \code{", "}.
}
\description{
Scans a directory tree for markdown drafts matching \code{pattern} and returns
a data frame summarising their frontmatter. Turns a compost-style
\verb{communications/} folder into a searchable archive without querying
Gmail.
}
\details{
Files without YAML frontmatter are included with \code{NA} for all metadata
columns so missing drafts are visible in the index rather than hidden.
}
\examples{
\dontrun{
mc_md_index("communications/")
mc_md_index() |> dplyr::filter(grepl("cindy", to))
}

}
